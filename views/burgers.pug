extends layout

block vars 
  -var title = "Burgers";

block content
  each item in burgers.expressburgers
    div(class='foodbox')
      img(src="#{item[1]}" class='foodpics')
      h3 #{item[0]}
      p #{item[1]}